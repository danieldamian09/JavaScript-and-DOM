// Obtener elementos por una clase
const items = document.getElementsByClassName('list-group-item');
// console.log(items)

// Iterar los elementos de Items

for (let i = 0; i < items.length; i++) {
  const eleme = items[i];
  // console.log(eleme);
  
}

